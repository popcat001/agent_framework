/**
 * Stub file — provides no extra pages when no parent project is detected.
 * When a parent project exists, Vite redirects @project/pages to the
 * project's web/pages/index.ts instead of this file.
 */
import type { PageDefinition } from "../types/pages";

export const pages: PageDefinition[] = [];
