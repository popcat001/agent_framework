/**
 * Stub file — provides empty overrides when no parent project is detected.
 * When a parent project exists, Vite redirects @project/config to the
 * project's web/project-config.ts instead of this file.
 */
export const projectOverrides: Record<string, unknown> = {};
